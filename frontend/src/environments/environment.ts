export const environment = {
  production: false,
  // Base URL of the Spring Boot backend.
  apiBaseUrl: 'http://localhost:8080/api',
  // Static API token for the single HR persona (matches backend app.security.api-token).
  apiToken: 'dev-hr-token',
};
